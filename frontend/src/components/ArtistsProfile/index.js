// import { useParams } from "react-router-dom";

function ArtistsProfile({users}) {
    // const { userId } = useParams();
    // const user = users.find(user => user.id === userId)
  return (
    <>
      <h2>artist page</h2>
    </>
  );
}
export default ArtistsProfile;
